'use client'

export default function Topbar() {
  return (
    <header className="w-full h-16 border-b border-zinc-800 px-6 flex items-center justify-between bg-zinc-900">
      <div className="text-lg font-semibold">Painel do Usuário</div>
      <div className="text-sm text-gray-400">Olá, Sávio 👋</div>
    </header>
  )
}